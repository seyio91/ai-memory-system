# Working — claude-memory-system

## Cross-project learnings (pending promotion)

_(none yet)_

## Checkpoints

### 2026-06-15 — renovate-manager: live-test hardening + memory/executor refinements

**Task:** Harden the (already-built, plan-complete) `renovate-manager` skill through live PR testing and structural refinements — memory layout, subagent context discipline, terraform tier semantics. (Full design now consolidated in `memory.md` → `renovate-manager` Architecture Decision.)

**Done:**
- **Live-validated both surfaces via actual subagents:** PR #335 (argocd/helm, trivy-operator 0.23→0.33.1) and PR #78 (terraform module, lambda 7.21.1→8.8.0). Memory built/updated correctly; #78 re-run was a package-cache hit → Subagent B skipped. Exercised Claude `Agent`, Codex `--executor`, and Codex `--executor-bare`.
- **Subagent context discipline made mandatory** (SKILL.md): orchestrator loads only the small body + changed-file paths; the full diff/clone/`terraform validate`/release-fetch happen **inside** the subagent, which returns only the compact contract. Saved as [[feedback-subagent-context]].
- **`codex-mem.sh --executor-bare`** added — strips the AGENTS.md memory stack via `-c project_doc_max_bytes=0` (~13.6k tok); stripped Codex = 20.4k tok (= Claude Agent). Review subagents → `--executor-bare` primary / Claude `Agent` fallback. Sandbox blocks `rm -rf` → subagents use a workspace-relative temp + `rm -r`.
- **Terraform tier = package-only**: consuming module is a `terraform validate` target (every affected folder's `example/`, no silent skip), results under the package tier's *Validated consumers*; no terraform project tier. Helm keeps package + project tiers.
- **Project-tier read→compare→update + first-run bootstrap** wired; project resolved from the repo URL, **ask-don't-assume** (no slug fallback).
- **Memory layout:** project tier under `helm/projects/<project>/<unit>.md` (sibling to `helm/charts/`); terraform = `terraform/{modules,providers}/`.
- **Surface fixes:** optional `Type` → infer from changed files; new **`argocd`** surface + `$values` valueFiles resolution; boilerplate chart-repo → app-repo changelog fallback.

**Next:**
- Optional: archive lifecycle (`/plan-archive renovate-manager-skill` + `/todo-archive` — todo fully ticked).
- Live-validate the two unexercised surfaces (GitHub Chart.yaml; terraform `provider` bump) when a real PR exists.
- Optionally delete the stale original at `~/Downloads/skills 2/review-renovate-pr/`.

**Blockers:**
- None. (`az` PAT/auth unverified for Azure DevOps PRs; `bkt` token ~2h windows.)
