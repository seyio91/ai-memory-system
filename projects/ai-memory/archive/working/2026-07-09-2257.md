# Working — ai-memory

## Cross-project learnings (pending promotion)

_(none pending)_

### ✅ GRADUATED — Executor deny-list enforced at the tool gate (shipped 2026-07-07, antigravity-hook-archetype Phase 2)
The tool-gate enforcement idea shipped: `harnesses/antigravity/hooks/pretooluse.sh` (PreToolUse guard,
`AI_MEMORY_ROLE`-gated) + `scripts/deny-list.txt` (deny-list as a shared shipped artifact). Antigravity is
now the 2nd enforced executor after codex, with a real `explore` read-only mode. Documented in
`docs/harnesses/antigravity.md` (§Enforcement) + `docs/harnesses/adding-a-harness.md`. The multi-harness
manifest `guard` **capability** remains the deferred generalization (build when a 2nd hook-capable harness
needs it). No `domain/` page: this is ai-memory system-architecture, not cross-project reusable knowledge.

## Checkpoints

### 2026-07-06 — Harness-agnostic plan: Phases 1–5 executed (P5 in review)

**Task:** Execute `make-memory-engine-harness-agnostic` phase by phase — turn `install.sh` into a generic, manifest-driven engine and prove it with a real third-party harness (Antigravity).

**Done:**
- **Phases 1–4 merged** (PRs #16–#19): content-core extraction (`scripts/content-core.sh` + `formatters/{xml,md}.sh`); layout restructure (`claude/`→`harnesses/claude/`, codex scripts→`harnesses/codex/scripts/`, `MEMORY_DIR` depth `../../..`); manifest engine (`scripts/manifest.sh`, `validate-manifest.sh`, `drivers/{hook,file}.sh`, `install.sh` rewrite with `--harness`/auto-detect/`--list`); skills+commands surfaces (`link-command-skills.sh`, `gen-commands-doc.sh`, archetype gate lifted).
- **Phase 5 built + PR #20 open (unmerged):** registered Antigravity (`harnesses/antigravity/manifest` + `scripts/agy.sh`), extracted shared `scripts/build-context-md.sh` (codex-mem.sh + agy.sh both call it; codex golden byte-identical), docs rewritten (new `docs/harnesses/antigravity.md` + `adding-a-harness.md`), reversed no-bootstrap non-goal. Suite 26/26.
- Every phase: claude wiring byte-identical (golden), live install verified, phase-per-PR.
- Earlier this session: brainstormed + folded the **two-face registry** (deliver + execute) into the plan; captured the tool-gate enforcement idea (see Cross-project learnings above).

**Next:**
- Merge PR #20, then sync main + delete branch (usual post-merge dance).
- **Phase 6** — de-brand the project marker `.claude/memory-project` → `.agents/memory-project` (walk-up detection in `memory_common.sh`/`_lib.sh`, `memory-pin.sh` writer, `lint-memory.sh`, codex adapter; back-compat fallback; migrate existing pins).
- **Phase 7** — executor roles (`task`/`exploration`) + `harness[:model]` config; `executor.sh` reads the manifest `exec_*` block (deletes the codex special-case at `executor.sh:38`) → this is where the Codex + Antigravity **executor** faces go live.
- Optional (offered, awaiting user): capture "why Antigravity, not Codex, is the generalization proof" in the plan's design section.

**Blockers:** None blocking. Note: Antigravity `context_target=~/.gemini/config/AGENTS.md` is a **best-guess global path** — unverified against a live authed `agy` session (one-line manifest fix if wrong).

**Resume:** Plan is `projects/ai-memory/plans/make-memory-engine-harness-agnostic.md` (Phases 1–5 ticked). After #20 merges, start Phase 6 there; the marker path lives in `harnesses/claude/hooks/memory_common.sh:detect_project` and `scripts/_lib.sh:detect_active_project` — change `.claude/memory-project` → `.agents/memory-project` with a legacy fallback read.

### 2026-07-06 — Harness-agnostic plan: COMPLETE (Phases 6–7 shipped; all 7 done)

**Task:** Finish `make-memory-engine-harness-agnostic` — Phase 6 (harness-neutral marker) and Phase 7 (executor roles + manifest exec_*), closing out all 7 phases.

**Done:**
- **Phase 5 merged** (#20, `5bbceca`) — Antigravity registered; shared `build-context-md.sh`.
- **Phase 6 merged** (#21, `1c9ceb9`): marker `.claude/memory-project` → `.agents/memory-project` in both readers (`_lib.sh:detect_active_project`, `memory_common.sh:detect_project`) with legacy fallback; writer `memory-pin.sh` migrates on pin; lint WARNs on legacy; **new `scripts/migrate-marker.sh`** (dry-run/`--apply`). **Ran `migrate-marker.sh --apply` on the real tree — 14 checkouts migrated, 0 legacy warnings left.**
- **Phase 7 built + PR #22 open (`69b9a4a`, unmerged):** `executor.sh` rewritten — `--role task|explore`, `harness[:model]`, resolves from manifest `exec_*` (`exec_cmd`/`exec_readonly`/`exec_probe`), **codex special-case deleted**; `explore` read-only degrades to Explore; model threading + `subagent:<model>`; `$MEMORY_DIR` expansion; `AI_MEMORY_HARNESSES_DIR` test override. codex/antigravity manifests gained `exec_*`; docs (identity.md, CLAUDE.md O/E/V, config.local.sh.example). `test_executor.sh` 23→37; suite 27/27; legacy configs unchanged.
- **Plan marked `status: done` / `completed: 2026-07-06`**; top banner updated. Two `/checkpoint` runs this session.

**Next:**
- Merge PR #22, sync main, delete branch.
- **Close-out housekeeping:** archive plan → `archive/plans/`, roll fully-ticked `todo.md` → `archive/todos/`, mark backing task (`38ef6850…`, notion) done.
- Optional (offered): fold the "why Antigravity, not Codex, is the generalization proof" note into the plan's Design before archiving.

**Blockers:** None. Deferred (noted in plan, not blocking): Cursor `.mdc` override example; Antigravity `hooks.json` upgrades (enforced read-only / live refresh / deny-list — see Cross-project learnings above); `doc`-surface content-core injection; Antigravity `context_target` best-guess unverified against a live authed `agy`.

**Resume:** All 7 phases shipped (PRs #16–#22; #22 open). After #22 merges: `/plan-archive make-memory-engine-harness-agnostic`, `/todo-archive`, mark the notion task done. The engine is complete — `install.sh` manifest-driven, 3 harnesses registered, marker neutral, `executor.sh` role/manifest-resolved with no per-harness code.

### 2026-07-06 — Harness-agnostic CLOSED + new plan: antigravity-hook-archetype (brainstormed, filed)

**Task:** Close out the harness-agnostic plan, then brainstorm & file the follow-up — make Antigravity a `hook` archetype (live injection + PreToolUse enforcement).

**Done:**
- **Harness-agnostic plan CLOSED.** #22 merged (`3d0fbe8`); main fast-forwarded; branch deleted. Close-out committed (`88bd7ba`): plan → `archive/plans/`, `todo.md` rolled → `archive/todos/2026-07-06-harness-agnostic.md`, `memory.md` refreshed (new "Harness-agnostic, manifest-driven engine" decision; marker + executor decisions updated; Codex adapter = registered harness).
- **Answered "use the project with Antigravity":** `install.sh --harness antigravity` (additive; doesn't touch Claude), alias `agy`→`harnesses/antigravity/scripts/agy.sh`; the `.agents/memory-project` marker is shared so same project/state carries across.
- **Brainstormed + FILED new plan `plans/antigravity-hook-archetype.md`** (committed `5bf66ed`; linked in todo.md). **Design locked** (see plan Decisions):
  - **Inject = pure hook.** `PreInvocation` script, cwd-resolved per call (like Claude `UserPromptSubmit`), `invocationNum==1`→full / else→breadcrumb via `content-core.sh`; **drop the built AGENTS.md**, keep `AGENTS.local.md` overlay as static base (= CLAUDE.md analogue). Chosen over hybrid because the built AGENTS.md's only home is a global `~/.gemini/config/AGENTS.md` (single-project clobber, unverified) — pure hook sidesteps the location/concurrency problem.
  - **Enforce = one `PreToolUse` guard, executor-delegations-only** (gated on `AI_MEMORY_ROLE`): always-on deny-list for executor sessions + write-deny when `explore` → gives Antigravity a real `exec_readonly`. Interactive `agy` unguarded. Deny-list = shared shipped artifact.
  - Phases: 0 (probe `agy` tool catalog + `hooks.json` location — prereq) → 1 (PreInvocation hook, flip manifest `archetype=hook`) → 2 (PreToolUse guard + `exec_readonly`) → 3 (docs).

**Next:**
- **User asked to checkpoint then compact the context** (done — this checkpoint).
- Start **Phase 0** of antigravity-hook-archetype: probe `agy`'s write-tool catalog (only `run_command`/`view_file`/`browser_*` known) + confirm `hooks.json` install location (workspace `.agents/hooks.json` vs global `~/.gemini/config/`).
- Still pending: mark the notion task `38ef6850…` done (harness-agnostic close-out).

**Blockers:** None. Phase 0 gates the enforcement work (tool catalog unknown). `ephemeralMessage` persistence + `hooks.json` discovery location need live-`agy` verification.

**Resume:** New plan `projects/ai-memory/plans/antigravity-hook-archetype.md` (status draft, design locked). Start Phase 0 — probe `agy` tool names via `~/.gemini/antigravity-cli/builtin/skills/` docs or a live session; the `PreInvocation`/`PreToolUse` contracts are in `~/.gemini/antigravity-cli/builtin/skills/agy-customizations/docs/hooks.md`. Reuse `scripts/content-core.sh` for injection selection; the hook driver (`scripts/drivers/hook.sh`) is Claude-shaped (stdout text) and needs a JSON-`injectSteps` variant for Antigravity.

### 2026-07-07 — antigravity-hook-archetype + statusline SHIPPED; skill-management-system plan filed (Phase 1 paused)

**Task:** Ship the Antigravity hook archetype end-to-end, add a memory-aware statusline, harden the Notion provider, refresh docs; then design & rephase the skills work into a Skill Management System.

**Done (all merged to main):**
- **antigravity-hook-archetype CLOSED** (Phases 0–3, PRs #24/#25/#26): live `PreInvocation` injection (`harnesses/antigravity/hooks/preinvocation.sh`; `agy.sh` exports `AI_MEMORY_PROJECT`/`AI_MEMORY_CWD`/`MEMORY_DIR` because the payload has NO workspace handle; `invocationNum` is **0-based**); `PreToolUse` guard (`pretooluse.sh` + `scripts/deny-list.txt`, `AI_MEMORY_ROLE`-gated, read-only allowlist for explore) → real `exec_readonly`; hook driver generalized to a `hooks_json` JSON-merge register; docs. Plan archived, todo rolled, notion `38ef6850…` marked done. Verified LIVE against real `agy`.
- **Memory-aware statusline** (PR #27, `harnesses/antigravity/statusline.sh`): reads agy's stdin JSON, shows project+folder+branch+model+ctx%+state; registered via idempotent `settings.json` merge (`statusline_settings`/`statusline_script` manifest keys + `_hook_register_statusline`). **Emoji by default** (Claude uses 🧠📁🌿; Nerd Font glyphs showed as boxes → `USE_NERD_FONTS=true` opt-in). Wired live. `tasks`/`subagents` in it are agy's internal counts, NOT memory todo.
- **Notion full-UUID guard** (`notion.py:_require_full_ref` on get/update/set_status; committed to main): short 8-char ids were API-rejected + ambiguous. Expanded memory.md refs to full UUIDs.
- **README refreshed** (`claude/`→`harnesses/`, Antigravity = hook harness, statusline bullet); `docs/system-overview.md` written for external review + **gitignored** (`/docs/system-overview.md`).
- **Installed the antigravity harness for real** (`install.sh --harness antigravity`): both hooks in `~/.gemini/config/hooks.json`, statusline in `~/.gemini/antigravity-cli/settings.json`, skills fanned to `~/.agents/skills`. (User handles the `agy` alias themselves.)
- **Captured backlog tasks:** manifest `guard` capability (`396f6850…81b2`), versioned zip release/packaging (`396f6850…8132`), statusline memory-todo count for Claude+agy (`396f6850…81a4`), Codex statusline (`396f6850…8146`).
- **Skill Management System plan FILED** (`plans/skill-management-system.md`, committed `296d8c3`; task `396f6850…814c` rescoped+started). Rebundled from "local vs generic skills" after the user surfaced the **source axis**. Design locked (brainstorm): **scope** = folders (`skills/` generic vs wholesale-ignored `skills-local/`; location is the signal) × **source** = declare-not-fork (split manifests `skills/skills.json` tracked + `skills-local/skills.json` gitignored → gitignored `~/.claude-memory/.skill-cache/` + lockfile). Centralized `_lib.sh:list_skill_dirs` over all 3 roots. 5 phases.

**Next:**
- **Start Phase 1 of skill-management-system** (user was about to confirm): add `list_skill_dirs()` to `_lib.sh` (roots `$MEMORY_DIR/skills:$MEMORY_DIR/skills-local`, `AI_MEMORY_SKILL_ROOTS` override, SKILL.md-gated); route `link-skills.sh` (source `_lib`, iterate the helper, build symlinks from full dir path) + `validate-skills.sh` (already sources `_lib`; replace its two `$SKILLS_DIR/*/` loops) through it; add `/skills-local/` to `.gitignore` with a self-documenting un-ignored `.gitkeep`. Verify with a throwaway `skills-local/` skill (fans out + git-ignored).

**Blockers:** None. (The Phase-1 `_lib.sh:list_skill_dirs` edit was written then reverted when we paused to rebundle — re-add it under the new plan.)

**Resume:** Open `scripts/_lib.sh` — add `list_skill_dirs()` right before `detect_active_project` (mirror the `skills_with_partial` idiom; split `AI_MEMORY_SKILL_ROOTS` on `:` via `set --`, glob `"$root"/*/` gated on `SKILL.md`). Then `link-skills.sh:51` (main loop) + `validate-skills.sh:79` (main loop) + their `--list` branches route through it. Plan: `projects/ai-memory/plans/skill-management-system.md` (Phase 1 = scope foundation). ~11 scripts glob `skills/` — only link-skills + validate-skills in Phase 1; the rest in Phase 2.

### 2026-07-07 — skill-management-system SHIPPED & CLOSED (all 5 phases)

**Task:** Build and ship the full skill management system — scope (generic/local) × source (authored/remote) — then close the plan out.

**Done (all merged to main, PRs #28–#32):**
- **Phase 1 (#28)** — `skills-local/` scope + centralized enumeration: `_lib.sh:skill_roots` / `list_skill_dirs`; `link-skills.sh` + `validate-skills.sh` routed through it; `/skills-local/*` gitignored with tracked `.gitkeep`.
- **Phase 2 (#29)** — authored-local: `new-skill --local` / `install-skill --from --local`; `apply-partial`/`skill-ratings`/`skill-boundary-check` made multi-root (`resolve_skill_dir`, `skills_with_partial` scans all roots); `fiter-infrastructure-analyzer` migrated to `skills-local/`.
- **Phase 3 (#30)** — remote source layer: `resolve-skills.sh` (sparse+shallow fetch → gitignored `.skill-cache/<name>/`, pinned by `skills.lock`; cache-hit-offline, hard-fail on a fetch that must run); `.skill-cache/` = third root. **Manifest = TOML** (stdlib `tomllib`, pip-free — folded into the branch so JSON never shipped).
- **Phase 4 (#31)** — config-driven authoring: `install-skill --remote --save` write-back to the manifest + resolve; `list-skills.sh` + `/list-skills` (derived provenance); `install.sh` resolve step; `sync-system.sh --update`; `_lib.sh:skill_manifest`. **"Vended" dropped** — two categories only (local authored / remote referenced; touch it → local, reference it → remote).
- **Phase 5 (#32)** — docs (`docs/harnesses/claude.md` "Skill scope & source" subsection; `knowledge-lifecycle.md` pointer; `scripts.md` env vars; `install.md` layout) + edge tests (`--update` re-pin; `--force` duplicate append).
- **Closeout:** plan `done`/`completed` → `archive/plans/`; todo snapshotted to `archive/todos/2026-07-07-skill-management-system.md` + reset; Notion task `396f6850-…814c` marked done; architecture decision (**Skill scope & source model**) recorded in `memory.md`; stale `fiter` gitignore ref fixed.
- Design decisions locked in review: **TOML over YAML/JSON** (bar = "python3 stdlib, no pip"; the task provider proves it); **per-skill manifest entries** (user maintains the explicit list, not whole-repo); **sparse+shallow fetch**; **hard-fail offline** (with cache-hit reprieve).

**Next:**
- **Operational migration (user-driven):** push portable skills to a separate git repo → per skill `install-skill --remote <url> --ref <tag> --path <subdir>` (add `--local` for per-instance) → remove the now-remote copies from `skills/` → `list-skills` verifies → `sync-system --update` keeps instances current.
- **Backlog (via `/start <ref>`):** manifest `guard` capability (`396f6850-…81b2`), versioned-zip packaging (`396f6850-…8132`), memory-todo statusline count for Claude+agy (`396f6850-…81a4`), Codex statusline+todo (`396f6850-…8146`), worktree/concurrent-sessions (`385f6850-…8020`).

**Blockers:** None — the system is shipped, tested (29 test files green), documented, and closed.

**Resume:** Nothing pending on skill-management-system — it's fully closed (`archive/plans/skill-management-system.md`). To continue, either drive the extract-to-separate-repo migration on the shipped mechanism (`install-skill --remote` per skill, then `sync --update`), or `/start` one of the backlog task refs above.

### 2026-07-08 — post-ship tier fix (boundary hooks multi-root) + enforcement design Q&A

**Task:** Fix a post-ship regression where `metadata.tier` enforcement ignored local/remote skills, and settle the design questions around remote-skill tier + harness-agnostic enforcement.

**Done (merged):**
- **Boundary-hooks multi-root fix — PR #33 merged.** `skill_boundary_marker.sh` (reads tier → arms read-only skills) and `skill_boundary_check.sh` (reads `.boundary-target`) both hardcoded `$MEMORY_DIR/skills/$name/…`, so a `target-read-only` skill in `skills-local/` or `.skill-cache/` had its `SKILL.md` not found, tier not read, boundary **silently never armed**. Fix: new `memory_common.sh:skill_dir_for` routes through `_lib.sh:resolve_skill_dir` (centralized enumerator, sourced lazily; `skills/` fallback); both hooks use it. Regression test in `test_skill_boundary_hooks.sh` (a `skills-local/` read-only skill now arms). 18/18 boundary, 29 files green. Phase 1–2 had routed the *scripts* through the enumerator but missed these two *harness hooks*.
- **Corrected a wrong claim I made:** the memory injection is NOT full-payload every prompt. `inject_memory.sh` emits the full `<memory:*>` payload only at SessionStart, first-prompt-post-compaction (sentinel), or when the prompt contains `@memory`; every other prompt gets a ~100-token breadcrumb. Verified by reading the hook. (Startup context ≈ 50–55k one-time; recurring per-prompt overhead is negligible.)
- **Design decisions locked (no code):**
  - **Remote-`tier` override → PARKED.** Trust boundary for referenced remote skills stays on the harness-agnostic **destructive floor** (Codex execpolicy / Antigravity deny-list) + **sha-pin** + **review-on-add**. `tier` is a *policy input to our detective git-diff boundary check*, not a sandbox.
  - **Soft-vs-hard enforcement principle (confirmed):** model-facing frontmatter (Claude `allowed-tools`, a self-declared `tier`) is advisory — the model can work around it; the deterministic gates are settings `deny`, `PreToolUse` hooks, and execpolicy. This is *why* the system deliberately uses a neutral `tier` label checked by **its own** mechanism (detective boundary check + floor), not Claude's `allowed-tools`. Per-skill tier boundary is currently Claude-only (Codex/Antigravity get the floor, not the per-skill check) — pre-existing, unchanged.

**Next:**
- Nothing in flight. Backlog via `/start <ref>`: manifest `guard` capability (`396f6850-…81b2`), versioned-zip packaging (`396f6850-…8132`), memory-todo statusline count for Claude+agy (`396f6850-…81a4`), Codex statusline+todo (`396f6850-…8146`), worktree/concurrent-sessions (`385f6850-…8020`).
- If the parked remote-`tier` override is ever revived: add optional `tier` to `[[skills]]` → `resolve-skills.sh` carries it through `_manifest_tsv` and **re-stamps** it onto the cached `SKILL.md` after the `cp` (survives `--update` because it's re-derived from the manifest each resolve, never stored in the disposable cache) → `install-skill --remote --tier`. Reuse the python frontmatter tier-normalizer already in `install-skill.sh`.

**Blockers:** None.

**Resume:** Clean `main`; no work in flight. For the parked remote-`tier` feature, the stamp point is `scripts/resolve-skills.sh:resolve_one` right after `cp -R "$src" "$dest"` (rewrite `$dest/SKILL.md` metadata.tier from the manifest); carry `tier` as a 5th TSV field in `_manifest_tsv`. Otherwise `/start` a backlog ref.

### 2026-07-08 — versioned-release-channel: Phases 1–2 shipped (PRs #40 merged, #41 open)

**Task:** `/start` the captured "versioned zip release" task, then execute the plan phase by phase.

**Done:**
- **Brainstormed the task down to its real shape.** The captured "ship a zip" was a *suggestion*.
  Decomposed into 4 sub-problems (stability / identity+rollback / change-communication / clean
  artifact). User scoped consumers to his own git-authed instances ⇒ clean-artifact evaporates ⇒
  **git tags ARE the package**. Rejected the zip + `.engine/<ver>/current` design (it solved
  no-git distribution, a problem that doesn't exist here) — full record in
  `projects/ai-memory/brainstorms/versioned-release-packaging.md`, incl. §7 the deferred
  external-user zip thread (revive: allowlist + leak-guard build, public-repo/history-scrub Q,
  graduate-to-git upgrade path).
- **Plan filed:** `plans/versioned-release-channel.md` — 5 phases, 8 success criteria.
- **Phase 1 (PR #40, merged, squash `02bb40c`):** `sync-system.sh` channel-aware.
  `AI_MEMORY_CHANNEL=release` (default) → latest **stable** `v*` tag; `dev` → ff-pull.
  `--to <ref>` ephemeral (tag/branch/sha). `semver_gt()` added to `_lib.sh`.
- **Phase 2 (PR #41, OPEN):** migration runner filling the placeholder. `migrations/<semver>-<slug>.sh`,
  gitignored `.applied-version` high-water marker, marker written after EACH success (resumable).
  `sort_v_supported`+`semver_sort_asc` moved/added to `_lib.sh` for Phase 3 reuse.

**Validator caught 4 rounds of real defects — the pattern is worth remembering:**
- P1: `--to` stranded **dev**-channel instances on detached HEAD forever (`@{u}` empty).
- P1: `dev_default_branch()` hardcoded `main`; a `trunk` clone without `origin/HEAD` aborted.
- P2: **duplicate migration versions** made a failed migration unresumable (marker ≥ its version) —
  fixed by rejecting duplicate versions outright.
- P2: **producer failure inside `while ... < <(cmd)` is swallowed** — a process substitution's exit
  status is never checked, `set -euo pipefail` does NOT catch it. Printed "No pending migrations",
  ran install.sh, exited 0.
Both P2 bugs were *silent-success*: install ran, exit 0, migrations skipped.

**Next:**
- Merge #41, sync main, delete branch, tick Phase 2 in `todo.md`.
- **Phase 3 — `release.sh`**: guards (dirty / not-on-main / failing suite / tag exists / version not
  monotonic) → finalize CHANGELOG section from `git log v<prev>..HEAD` → annotated tag + push.
  Tag-push is orchestrator-only, never an executor. Reuse `semver_sort_asc`/`semver_gt` from `_lib.sh`.
- Phase 4 docs (CHANGELOG.md, UPGRADING.md w/ the N/N+1 + forward-only rules, semver rule of thumb).
- Phase 5 cut `v1.0.0`, flip one real consumer instance, dogfood `--to main` + snap-back.

**Blockers:** None. `.applied-version` semantics assume the **N/N+1 compat rule** (migration must not
break the previous release's code) — that rule lives only in `migrations/README.md`; the runner
cannot enforce it. Validator is the backstop.

**Env gotcha hit this session:** macOS **TCC revoked read access to `~/Downloads/**` mid-session** —
`/bin/ls` (stat) worked but `/bin/cat`, `python3`, and `git` all got `EPERM` on file *contents*.
Disabling the Claude sandbox changed nothing (it's TCC, not the harness). Recovered on its own.
The memory tree living under `~/Downloads` is the root cause; moving it to e.g. `~/dev/ai-memory`
+ re-running `install.sh` would retire this class of failure permanently.

**Also:** this dev checkout now has `AI_MEMORY_CHANNEL="dev"` in gitignored `config.local.sh` —
written BEFORE Phase 1 landed so the new `release` default could never flip the working tree onto a tag.

**Resume:** `main` = #40 squashed + Phase-1 todo tick. `feat/release-channel-phase2` = PR #41 (2 commits).
After #41 merges: `git checkout main && git pull --ff-only`, delete branch, tick Phase 2 in todo.md,
then start Phase 3 in `scripts/release.sh` (new file).

### 2026-07-08 — versioned-release-channel CLOSED (v1.0.0 + v1.1.0 shipped, consumer flipped)

**Task:** `/start` the captured "versioned zip" task → brainstorm → 5 phases → cut releases → close out.

**Done (PRs #40–#46, two tags, one day):**
- Brainstormed the zip away. Consumers = the user's own git-authed instances ⇒ clean-artifact
  problem evaporates ⇒ **a git tag IS the release**. Rejected zip + `.engine/<ver>/current`.
- P1 channel + `--to`; P2 migration runner + `.applied-version`; P3 `release.sh`;
  P4 CHANGELOG/UPGRADING + enforced per-version test; P5 cut `v1.0.0`.
- **#44** `git tag -a -m` strips every `#` line (default `--cleanup=strip`) → all markdown
  headings silently deleted from tag messages. Retagged `v1.0.0`.
- **#45** `identity.md` was tracked while `install.sh` tells you to edit it → dirty guard
  bricked every personalised instance. Untracked it.
- **#46** stale docs: UPGRADING claimed no tags existed; `/sync-system` described the
  pre-Phase-1 script for two days; no conversion runbook existed.
- `v1.1.0` cut. Consumer instance flipped. Plan/brainstorm archived, todo rolled, notion done.

**The five things worth carrying forward (all now in memory.md):**
1. `git tag -a -m` strips `#` lines. `git tag -l -n` reformats and hides it — only
   `git cat-file tag` shows the raw object.
2. A tracked file the user is told to edit is a latent sync-brick. Removing a tracked file
   is data loss the migration runner **structurally cannot cover** (it runs after checkout).
3. `v1.0.0` is a trap tag: checking it out silently overwrites a personalised `identity.md`.
4. A nonzero exit inside `while ... done < <(producer)` is **silently swallowed** under
   `set -euo pipefail`. Materialize with `x="$(producer)" || abort`.
5. `config.local.sh` is sourced *after* the env, so it **beats** an env prefix.

**Process notes:**
- Same-model validation (codex validating codex) caught 4 real bugs across P1–P3 but **missed**
  the tag-message bug — 107 assertions verified a tag was *created*, none verified what it *said*.
  That is direct evidence for backlog task `397f6850-…0596` (cross-model validator).
- Docs rot within hours when phases ship serially. Nothing tests a command doc against the
  script it invokes.
- I lost my own `identity.md` to `git reset --hard origin/main` an hour after documenting that
  exact failure — because the note said "sync", and the trigger is *any* checkout.

**Next:** open-sourcing is the goal. Live threads: release automation via changelog fragments
(`397f6850-…dd65`, design in `brainstorms/release-automation.md`), task-provider `body` field
(`397f6850-…8fbf`), `@`-sign section loading (`38ff6850-…4bf2`), cross-model validator
(`397f6850-…0596`).

**Blockers:** none. **Warn Napho + Plork before they `git pull`** — their tracked `identity.md`
is silently replaced by the template; recover with `git show v1.0.0:identity.md > identity.md`.

### 2026-07-09 — cross-model-validator built + PR #47 open (awaiting merge → 1.2.0 batch)

**Task:** `/start`ed the captured cross-model validator task (`397f6850-…0596`), 4 phases, branch + PR.

**Done:**
- Brainstormed → **rejected the task's own manifest `exec_family`/`exec_tier` registry** (family+tier are model properties, not harness → a hand-maintained tier table rots). Chose the **structural default**: `validate` role defaults to the orchestrator's own agent plane, which is guaranteed a different family from any CLI executor — zero metadata. Design/decisions in `plans/cross-model-validator.md`.
- **P1** `executor.sh`: `validate` role; `role_value()` maps → `AI_MEMORY_EXECUTOR_VALIDATE`, unset → `claude-subagent` with **no legacy/`_TASK` chaining** (the whole point — chaining would re-correlate models); `resolve_value()` routes `explore|validate` through `exec_readonly` (read-only, degrade-to-subagent); `--role`/`--show`/usage. Codex executed it; I fixed the `--show` column alignment by hand after.
- **P2** `pretooluse.sh`: read-only allowlist gate widened `explore` → `explore|validate`.
- **P3** tests: `test_executor.sh` 41→51, `test_antigravity.sh` 49→56; suite 32 files/0 fail.
- **P4** doctrine across identity.md, docs/{workflow,system-overview,scripts,showcase}.md, config.local.sh.example, harnesses/claude/CLAUDE.md, memory.md (Architecture Decision) + removed the backlog note. Swept for stale "same executor, re-invoked" phrasing — clean outside archive/.
- **Dogfood validation:** `--role validate --which` → `subagent` (unset, cross-model vs the codex executor). Ran a fresh Claude subagent validator → **all 9 success criteria PASS, no bugs**.
- Branch `feat/cross-model-validator`, commit `476ca6e`, **PR #47** open; CHANGELOG `## [Unreleased]` seeded with the `### Added` entry.

**Next:**
- Merge #47 → sync main → delete branch. Task `397f6850-…0596` stays `started` until then.
- **User wants a 1.2.0 batch** — bundle this + a couple more backlog tasks, then one `scripts/release.sh 1.2.0`. Ask which other tasks; each gets its own branch/PR + `## [Unreleased]` entry before cutting.
- **Out-of-tree:** user-global `~/.claude/CLAUDE.md` still states the old validator doctrine (repo copy fixed; the global one isn't tracked) — patch manually or via a sync pass. User hasn't decided.

**Blockers:** none.

**Resume:** `feat/cross-model-validator` = PR #47 (1 commit). Plan `plans/cross-model-validator.md` (all phases + criteria ticked). After merge: standard close-out is deferred to the release batch — don't archive the plan yet; it ships in 1.2.0 alongside sibling tasks.

### 2026-07-09 — validator #47 merged + #48 follow-ups; global CLAUDE.md de-drifted; prevention tasks + wiki filed

**Task:** Land the cross-model validator, then act on a system-review's validator-affecting findings and the drift root-causes behind them.

**Done:**
- **PR #47 MERGED** (`c0de9f0`, squash) — the cross-model `validate` role + the install-guidance `@`-import fix. main synced, `feat/cross-model-validator` deleted.
- **Global `~/.claude/CLAUDE.md` refactored to a thin shim** that `@`-imports `harnesses/claude/CLAUDE.md` (via the `~/.claude-memory` symlink) + keeps `@RTK.md`. It was a merged Jul-1 copy that had drifted (stale validator doctrine); now it tracks the repo automatically. Backup: `~/.claude/CLAUDE.md.bak-2026-07-09`. Root cause was `drivers/hook.sh` telling installers to "symlink or merge" — fixed in #47 to recommend `@`-import.
- **Reviewed `projects/ai-memory/wikis/2026-07-09-system-review.md`** (a 4-agent review, NOT authored this session — appeared untracked). Extracted the validator-affecting findings → A (model-suffix leaks on read-only degrade), B (README + antigravity.md miss `validate`), C ("capability floor" overclaims a mechanism).
- **PR #48 OPEN** (`fix/validate-role-followups`) — A: `executor.sh` clears `R_MODEL` on degrade + regression test (both explore & validate); B: doc fixes; C: reworded across workflow.md/config.local.sh.example/harnesses/claude/CLAUDE.md/memory.md. Suite 32 files green, test_executor.sh 51→53. Added a `### Fixed` CHANGELOG entry (explore shipped the leak in 1.1.0).
- **Prevention infra captured as tasks:** shellcheck gate (`397f6850-…873ad`) + doc-vs-code consistency test (`397f6850-…1bfe6`). Filed `domain/preventing-drift.md` (indexed) capturing the 3 defect classes→controls, single-sourcing, and "a validator only checks enumerated criteria."
- **Explained the release flow** (tag-IS-the-release; feature→branch→PR→merge; `## [Unreleased]` accumulates; one `release.sh 1.2.0` cuts the batch). Fragments+Actions automation is still backlog (`397f6850-…dd65`), not built.

**Next:**
- Merge PR #48 → sync main → delete branch.
- **Decide the 1.2.0 batch** — which backlog tasks ride with the validator. Strong candidates: the two prevention tasks (small, close this theme). Then cut `scripts/release.sh 1.2.0` once all are on main.
- Task `397f6850-…0596` (cross-model validator) stays `started` until the release ships; then close out (archive plan, roll todo, mark done).

**Blockers:** None. Note: `identity.md` is gitignored (per-instance) — Phase-4 doctrine edits to it only affected THIS instance; tracked doctrine lives in `harnesses/claude/CLAUDE.md` + `docs/workflow.md` (both current). This is itself an example of the doc-vs-code drift the prevention tasks target.

**Resume:** PR #48 (`fix/validate-role-followups`) awaiting merge; A's fix is `scripts/executor.sh` resolve_value degrade branch (`R_MODEL=""` on the `explore|validate` no-`exec_readonly` return). After merge, pick the 1.2.0 batch and cut via `scripts/release.sh 1.2.0` from clean main. Stray untracked `projects/ai-memory/wikis/2026-07-09-system-review.md` left in place (not mine).

### 2026-07-09 — shellcheck-gate: /start'd, mid-brainstorm, AWAITING user's approach decision

**Task:** shellcheck gate (`397f6850-c619-812d-8677-fff1cfe873ad`). `/start`ed → it's a Tier-3
feature with open design → ran the brainstorm gate. **Still `backlog`, NO plan yet** — paused for
the fail-vs-warn decision. Resume by presenting the decision below and, once chosen, scaffolding
`plans/shellcheck-gate.md` + flipping to `started`.

**BASELINE MEASURED (don't re-measure — shellcheck 0.11 IS installed here):**
- 83 `.sh` files under `scripts/` + `harnesses/`. Whole-tree: **145 findings — 0 error, 23 warning,
  122 info/style.**
- **76 are SC1091/SC1090** ("source not followed") — pure noise for the `. "$SCRIPT_DIR/_lib.sh"`
  pattern; disable via a repo `.shellcheckrc`. Leaves ~23 warning + ~50 info.
- **SC2015 × 9** = the `A && B`-as-last-statement bug class that aborted install.sh in the review —
  the gate would pay for itself on exactly its motivating bug. Other codes: SC2016×19 (mostly
  intentional single-quote), SC2034×8, SC2086×6, SC2030/2031 (subshell var), SC2044, SC2181.
- Measurement gotcha: `shellcheck -f json $files` returned empty under the Bash-tool zsh (arg
  quirk); `find … -exec shellcheck -f gcc {} +` is the reliable invocation. gcc format relabels
  severities: note = shellcheck info/style.

**THE PENDING DECISION (put to user on Telegram, msg 354):**
- **A. Baseline-freeze + burn down** — record today's findings as accepted, gate fails only on NEW.
  Zero upfront work; the 23 warnings linger.
- **B. Clean-to-floor then hard-fail (MY REC, scoped to warning)** — disable SC1091/SC1090, fix the
  23 warnings now (incl. SC2015), hard-fail at severity=warning, info/style advisory (printed not
  gating). Bounded cleanup; a net that would've caught the install.sh abort. Sub-question: fix the
  23 in THIS task or split into a cleanup phase.
- **C. Error-only gate** — hard-fail on `error` (0 today), rest advisory. Cheapest but too weak;
  the review's real bugs were warning/info level.

**Design facts already settled (any approach):** `.shellcheckrc` disables SC1091/SC1090; wire into
`scripts/run-tests.sh` as a stage (+ future CI); shellcheck is dev/CI-only so the zero-RUNTIME-dep
bet is untouched; inline `# shellcheck disable=SCxxxx` allowed for justified intentional cases
(e.g. the `set -f; set -- $1` deliberate word-split in executor.sh:35 `first_word`).

**Next when resuming:** get A/B/C from user → scaffold `plans/shellcheck-gate.md` (task_ref
`397f6850-…873ad`) with success criteria → push refined Goal back (≤500 chars, by name) → flip
`started` → add todo. If B: phase 1 = `.shellcheckrc` + fix warnings, phase 2 = run-tests stage,
phase 3 = CI + docs.

**Blockers:** the A/B/C decision (user paused here).

### 2026-07-09 — deny-list hardening DONE (7 rounds, PR #51 open, awaiting merge → 1.2.0)

**Task:** deny-list bypass (`398f6850-…6df6`), plan `deny-list-hardening` — resumed from the round-5
pause, fixed the open bypass + 2 more rounds, closed out.

**Done:** `scripts/deny-match.sh` tokenized matcher. Rounds 6-7 (Fable, cross-family) closed the last
bypasses: wrapper∘`sh -c` (`timeout 5 sh -c "terraform apply"`, `sudo -u root sh -c …`) via a
wrapper-tail payload scan; and a wrapper's OWN `-c` flag (`flock <lock> -c "terraform apply"`) via
flock/script `-c` extraction. **Total: 7 rounds, 24 bypass classes, 164 assertions, suite green.**
User called it sufficient hardening (no round 8 — remaining findings are obscure per-tool flags, past
the honest-agent threat model). Plan `status: done`. CHANGELOG + antigravity.md updated in lockstep.
Also gitignored stray `terraform.tfstate` from validator probes.

**Commits on branch:** `677837e` (WIP pause) → `15a46e0` (round-6 plan correction) → `d4afbda`
(round-6 fix) → `9498ad6` (round-7 fix + closeout). **PR #51 OPEN**, base main.

**THE lesson (promote-worthy):** a green test suite is the START of validating a security control,
not the end — all 24 bypasses passed a green suite first, and 3 were introduced by the fix for an
earlier finding. A fix that NARROWS a matcher to kill a false positive is the likeliest place to
create a false negative. Cross-*family* validation (Fable vs Opus/Sonnet) caught a blind spot shared
across the whole Opus family — including a bug in my own fix sketch. And: verify shell semantics by
PROBING bash (`echo PWNED`), never by reasoning (eval flattens quoting, find -exec preserves it —
opposite, one line apart; two of my "test failures" one round were my own wrong expectations).

**Next:** merge #51 → sync main → delete branch. Then the **1.2.0 batch**: on main will be validator
(#47/#48), summary-gate (#49), name-not-path (#50), deny-list (#51). Candidates to add: the 2 prevention
tasks (shellcheck `397f6850-…873ad`, doc-vs-code `397f6850-…1bfe6`). Then `scripts/release.sh 1.2.0`.
Task `398f6850-…6df6` stays `started` until the release ships.

**Blockers:** none.

### 2026-07-09 — deny-list hardening PAUSED after round 5 (WIP branch pushed, ONE open bypass) [SUPERSEDED above]

**Task:** `/start`ed the deny-list bypass (task `398f6850-c619-816d-b1f8-e57e2d046df6`, plan
`deny-list-hardening`). User asked to pause after round 5 at a resumable state. **NOT merged,
NO PR** — deliberately, because a bypass is still open.

**State:** branch `fix/deny-list-hardening`, commit `677837e`, pushed. Suite 34/34 green, 137
assertions in `scripts/tests/test_deny_list.sh`. `git checkout fix/deny-list-hardening` to resume.

**THE RESUME POINT (one open bypass, pre-existing, NOT a round-5 regression):**
```
timeout 5 sh -c "terraform apply"    -> ALLOW  (should DENY; probe-confirmed bash runs it)
flock /tmp/l sh -c "terraform apply" -> ALLOW
nice -n 10 sh -c "terraform apply"   -> ALLOW
```
Both halves are independently covered/tested (`timeout 5 terraform apply` DENIES, `sh -c
"terraform apply"` DENIES) — the **composition** slips through. Mechanism: `_deny_command_start`
skips a wrapper by advancing ONE token, landing on the wrapper's flag value (`5`), so `primary=5`
and the `sh -c` payload is never extracted; the tail-scan then can't match the quoted payload
token's basename. **Fix sketch** (in the plan's `## Open — round-5 finding`): after skipping a
wrapper, re-run head analysis from the first real command token, so a payload-bearing binary
(`sh -c`/`eval`/`find -exec`) in the tail gets its payload extracted. Add the 4 composed tests +
benign twins. **The `eval "$(echo terraform apply)"` case is an ACCEPTED limitation — do not fix**
(undecidable for a text-matcher; document like `echo terraform apply`).

**What shipped in the branch (all validator-confirmed correct):** `scripts/deny-match.sh` (new)
tokenized matcher — flag interposition, wrapper tail-scan, shell re-entry (sh -c/bash -lc/glued/
herestring/eval/trap/su -c/find -exec with argv-preserving requote), `$()`/backtick/`<()` with a
BOTH-quotes + `$'…'` quote model, depth cap denies. `pretooluse.sh` fails closed (no parser /
missing / rule-less spec, all after the role gate). `deny-list.txt` → spec format + helm
uninstall/delete. Gitignored additive-only `deny-list.local.txt` overlay (tracked defaults never
hand-edited → no sync-brick).

**THE lesson (recorded in the plan, worth promoting):** five rounds, 21 bypass classes, **every
one found after the suite was green**, and **3 of 21 were introduced by the fix for an earlier
finding**. A fix that narrows a matcher to kill a false positive is the most likely place to
create a false negative — neither direction is visible without an adversary. For a security
control, a green suite is the START of validation, not the end. Also: verify shell semantics by
PROBING bash (`echo PWNED`), not by reasoning — two of my "failures" one round were my own wrong
expectations (`eval` flattens quoting; `find -exec` preserves it — opposite, one line apart).

**ROUND 6 (Fable, user-requested different model family) — done, plan updated:**
- Confirmed the `timeout/nice/flock ∘ sh -c` bypass reproduces.
- **New:** `sudo -u root sh -c "terraform apply"` ALLOWs — same class on `sudo -u`, the most
  natural wrapper, whose bare form already has a passing DENY test (false confidence). Plus
  `stdbuf -oL`/`xargs -n1`/`watch -n5 ∘ sh -c`, stacked wrappers, wrapper→`su -c`.
- **Caught a bug in MY fix sketch:** it listed `eval`/`trap` in the wrapper-tail payload set and
  required `timeout 5 eval "terraform apply"` → DENY. WRONG — `eval`/`trap` are builtins with no
  on-disk executable, so a wrapper can't exec them (`nice eval …` → "No such file or directory",
  probe-verified). That command runs nothing; denying it is a FALSE POSITIVE. Corrected the plan:
  wrapper-tail set = `sh/bash/zsh/su/runuser` + `find -exec`, NOT eval/trap; `timeout 5 eval …`
  is now a required ALLOW twin.
- Tally now **six rounds, 23 bypass classes**. The Fable pass is the argument for cross-*family*
  review: a shared Opus/Sonnet blind spot (sudo; the eval-builtin fact) only surfaced under a
  different lineage.

**Next when resuming:** implement the wrapper∘payload fix per the CORRECTED sketch (exclude
eval/trap from the wrapper-tail set), add the ~10 named tests incl. the `timeout 5 eval` ALLOW
twin, run a 7th adversarial pass (prior-round-introduced-a-bug rate is 3/6 — do not trust a green
suite), then merge → 1.2.0 batch. Task `398f6850-…6df6` stays `started`.

**Blockers:** none — paused by user, not blocked.

### 2026-07-09 — name-not-path rule; #50 merged; branches clean

**Done since #49:**
- **Notion intake gap → resolved won't-fix, by design** (`e503ad1`). The page body is **outside the
  contract** — no reader, no writer (`notion.py` has zero `children`). Reading it would make the page
  a second home for detail, the split-brain the projection model forbids. Note the gap is *read*-side:
  not building the `body` **writer** does not close it; the projection model does. Documented so a
  human doesn't type a design into a Notion page body and watch it silently vanish. No task filed.
- **Dangling-pointer investigation → the convention changed instead of a linter being built.**
  Found live rot: `memory.md` claimed the release-packaging design record was at `brainstorms/…`
  (line 54) *and* `archive/wikis/…` (line 91). Root cause: `/plan-archive` moves the file; nothing
  rewrites references.
- **Architecture Decision — durable memory names artifacts, never their paths** (`8c11ed0`).
  A path rots on archival; a name doesn't. Reader resolves name → live dir → `archive/`. `archive/`
  survives in `memory.md` only as a *directory policy*, never as a filename. Only machine-consumed
  fields carry paths (plan frontmatter `task_ref`, `todo.md`→plan link) — those point at live files
  by definition. Rewrote 6 rotting refs (incl. `skill-subsystem`, `antigravity-*` plan records).
- **#50 merged (`12d0e7f`)** — the gate's own error text taught the outlawed habit ("reference it by
  path"). Now: reference **by name**; the task record already carries its `project`. Propagated to
  contract.py, the verbatim-pinned tests, docs, `/task`, `/start`, CHANGELOG, plan Decisions.
  Dogfooded: this task's summary now names `task-summary-gate` (452 chars). All `[Unreleased]` — the
  bad wording never shipped in a tag, so no migration.
- **No lint built.** Naming removes the archival-rot failure class; detecting a missing *named* file
  would need lint to stat `archive/`, brushing the hard rule for little gain. Deliberate non-goal.
- Branch hygiene: `fix/validate-role-followups` (merged, obsolete) and `feat/renovate-manager-remote`
  (predates the skill-source refactor — its `skills/skills.toml` path no longer exists; migration
  shipped via `install-skill --remote` on the instance) both deleted. Recovery ref for the latter:
  `60ad9b8b1b9873084e35eae3d2a1d187efa03258`. Only `main` remains, local and remote.
- **Verified the renovate skill migration**: remote in `skills.toml`, pinned `6c6c1b28d8` in
  `.skill-cache/skills.lock`, materialized in `.skill-cache/renovate-manager/`, **no fork** in
  `skills/`, review memory in `.skill-data/renovate-manager/renovate-reviews/{terraform,helm}/`.

**Next:** the **1.2.0 batch**. On main: validator (#47/#48), summary gate (#49), name-not-path (#50).
Candidates: shellcheck gate (`397f6850-…873ad`) + doc-vs-code test (`397f6850-…1bfe6`).
Then `scripts/release.sh 1.2.0` from clean main.

**Still open (nothing blocking):**
- **Two pre-public gates** (task `398f6850-…ace54`): deny-list flag-interposition bypass (verified
  live: `terraform -chdir=… apply`, `kubectl -n foo delete pod x` pass the guard) and live client
  data in `docs/demo-runbook.md`. These are what "prepare to open-source" actually reduces to.
- **`/checkpoint` → `add_progress` wiring**: never decided. Design no longer depends on it.
- Lint warns 3 stale `working.md` files in *other* projects (access-workloads, fiter-charts,
  fiter-infra-scaffolder).
- System-review wiki archives to `archive/wikis/` once the plans it feeds land.

### 2026-07-09 — task-summary-gate SHIPPED to PR #49 (all phases, 2 clean validator passes)

**Task:** `/start`ed the task-provider `body` task; the brainstorm rejected the task's own proposal.

**Done:** all phases. Branch `feat/task-summary-gate`, commit `8734666`, **PR #49 open**.
Task retitled → "Task summary: hard 500-char gate, long-form by pointer", summary pushed back
(471 chars — dogfooded the cap), status `started`. Plan criteria all ticked; todo phases ticked.

**Design:** cap `summary` at 500 at the `TaskProvider` ABC boundary (`validate_summary` applied by
`__init_subclass__` to `capture`+`update`, as `set_status` already is). Backend-neutral — the rule is
the projection model, not Notion's per-element limit — so it fires before dispatch and `local` obeys
it. Reads ungated (9 of 12 live tasks are over cap; corrected on next write, no migration). Long-form
→ `projects/<p>/brainstorms/<slug>.md` by pointer. Invariant narrowed to "no plan, no todo row, no
index entry" before `/start`.

**Rejected:** chunking `_text_value` (symptom not disease; and moot — ≤500 always fits one rich_text
element); a bidirectional `body` field; any Notion block writer. **`add_progress` is NOT dead** —
`docs/task-provider.md:30` documents it as "Designed, not wired", a deliberate no-op reserved for
`/checkpoint`. Left alone.

**The bigger find:** `run-tests.sh` globbed only `scripts/tests/test_*.sh`, so
`scripts/taskprovider/tests/` had **never executed** — `32 passed` before and after a new python test
file was added. Now a hermetic `== taskprovider (python) ==` stage gating the exit code, plus new
`scripts/check-provider-tests.sh` enforcing `providers/<name>.py` ↔ `tests/test_<name>.py`. Suite is
33 bash + 20 python.

**Validation:** two independent read-only cross-model passes (Claude subagent vs the codex executor),
**zero defects**. Pass 2 was mandatory, not ceremony: I refactored `run-tests.sh` after pass 1, so its
criteria 9-10 had been verified against code that no longer existed. Pass 1 also caught me
overclaiming — the plan said the pairing check was "verified by a fixture provider" when that fixture
only lived in a throwaway shell command; that became `test_provider_test_pairing.sh`.

**MERGED** — #49 squashed to `7fe99f0` on main; branch deleted (remote auto-deleted); stale
`origin/fix/untrack-identity` + `origin/fix/validate-role-followups` pruned. Suite green on main
(33 bash + 20 python). Architecture Decision + the ungated-suite gotcha recorded in `memory.md`.

**Next:** the **1.2.0 batch**. On main already: cross-model validator (#47/#48) + this gate (#49).
Candidates to ride along: shellcheck gate (`397f6850-…873ad`) + doc-vs-code consistency test
(`397f6850-…1bfe6`) — both close the same drift theme the ungated-suite bug belongs to.
Then `scripts/release.sh 1.2.0` from clean main. Close-out (archive plans, roll todo, mark the
notion tasks done) is deferred to the release, per convention.

Stale local branches left in place, not mine to judge: `feat/renovate-manager-remote`,
`fix/validate-role-followups` (its PR #48 is merged — safe to delete).

**Blockers:** none.

### 2026-07-09 — task-summary-gate: Phases 1-3 executed by codex, STOPPED before docs/validation (superseded above)

**Task:** `/start`ed the task-provider `body` task (`397f6850-c619-812b-a4a1-e996df808fbf`); brainstorm
rejected its own proposal; executing the plan that came out instead.

**Stopped by user mid-flight.** Branch `feat/task-summary-gate`, NOTHING committed or pushed.
Working tree: `contract.py`, `test_contract.py`, `test_taskprovider_cli.sh` modified; new
`tests/test_summary_gate.py`; new `plans/task-summary-gate.md`; `todo.md` has the 4 phases.

**Design (locked, in the plan):** cap `summary` at 500 chars at the `TaskProvider` ABC boundary via
the same `__init_subclass__` wrap that guards `set_status`. Write-only gate; reads ungated (9 of 12
live tasks are over cap and stay readable). Long-form → `projects/<p>/brainstorms/<slug>.md`, by
pointer. Rejected: chunking (moot — ≤500 always fits one rich_text element), the `body` field, any
Notion block writer. `add_progress` stays a documented no-op. Invariant narrowed to "no plan, no
todo row, no index entry" before `/start`.

**Done:** Phases 1-3. Codex added `SUMMARY_MAX_CHARS`, `validate_summary()`, separate wrappers for
`capture` (positional summary) and `update` (kw-only). Error text:
`summary is <n> chars; maximum is 500. Write long-form content to projects/<project>/brainstorms/<slug>.md and reference it by path from the summary.`
Dogfooded: the refined summary pushed back at `/start` is 471 chars.

**⚠ Unverified — I stopped before the Validator ran.** Codex self-reported green; nothing independent
has checked it. Plan success criteria are all still unticked.

**⚠ Real finding from the executor:** `scripts/run-tests.sh` does NOT discover
`scripts/taskprovider/tests/` — the Python unittest suite is outside the gate entirely. Suite stayed
`32 passed` before and after adding `test_summary_gate.py`. Run manually:
`PYTHONPATH=scripts python3 -m unittest discover -s scripts/taskprovider/tests -t scripts`.
This is the doc-vs-code drift theme again: a whole test suite believed to be gated, isn't.
Worth its own task.

**Next:** Phase 4 (docs — `docs/task-provider.md` gate section + narrowed invariant, `/task`, `/start`,
CHANGELOG). Then run the `validate` role (resolves to `subagent`, cross-model vs codex) against the
plan's 8 success criteria. Then branch → PR (feature work, not housekeeping).

**Blockers:** none — stopped by user, not blocked.

### 2026-07-09 — #48 merged; system-review wiki committed + captured as a task

**Done:** PR #48 merged (`06fdabc`); main synced, stale `fix/validate-role-followups` left undeleted.
Committed the four-agent system review (`8468d97`, pushed) — trackable via the `!/projects/ai-memory`
carve-out, redaction verified clean. Captured **one** follow-up task `398f6850-c619-8153-a9be-caaf092ace54`
pointing at the wiki rather than restating it.

**Two pre-public gates** (from the wiki, not yet fixed):
1. **Deny-list flag-interposition bypass, verified live** — `terraform -chdir=… apply`, `kubectl -n foo
   delete pod x`, `gh --repo o/r pr merge 12` all pass the guard (`scripts/deny-list.txt:11-19` matches
   binary-immediately-followed-by-subcommand). Pushing this wiki publishes a working bypass of our own
   executor guardrail. Also missing `helm uninstall`/`helm delete`. Related: Antigravity deny-list
   **fails open** with no jq/python3.
2. **Live client data in the tracked tree** — `docs/demo-runbook.md:21,87,98,173` (real machine username +
   client repo path). Falls under remove-going-forward; the `archive/` client names stay per keep-history.

**Next:** decide the 1.2.0 batch. The review's priority order maps onto it — deny-list fix + manifest
`guard` capability close the enforcement theme; the doc-vs-code test closes the drift theme.

**Blockers:** none.

