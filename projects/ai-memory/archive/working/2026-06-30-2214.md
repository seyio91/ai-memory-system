# Working — ai-memory

## Cross-project learnings (pending promotion)

_(none yet)_

## Checkpoints

### 2026-06-30 — POS adoption triage + skill-subsystem design
- **Task:** review POS memory system, decide what to adopt, file plans.
- **Done:** wikis `pos-comparison.md` + `pos-adoption-backlog.md` (13 items triaged — approved: #1,4,5,6,7,8,10,11,12,13; rejected: #2 QUICK-START, #3 tiered-loading, #9 declarative-onboarding). Two plans filed: `plans/skill-subsystem.md` + `plans/state-snapshot.md`. todo.md populated. Memory pointer added under Current Goal. Brainstorm settled the #11 seam (Approach A: shared `skill-boundary-check.sh` + Codex/`codex-mem.sh` trigger + Claude Skill-`PostToolUse`-marker/`Stop`-hook trigger; split enforcement target-tree-universal / memory-confinement executor-full+in-session-narrowed).
- **Next:** skill-subsystem Phase 3 (boundary check #11, seam settled Approach A — shared `skill-boundary-check.sh` + Codex/`codex-mem.sh` trigger + Claude Skill-`PostToolUse`-marker/`Stop`-hook trigger; split enforcement). Open sub-qs: multi-turn marker-clear rule, SubagentStop wiring. Then Phase 4 (creator/installer #12/#13), Phase 5 (self-rating #6/#5). #1 & #7 are inline quick wins, independent.
- **Phase 1 DONE (skill-subsystem):** `metadata.tier` added to all 12 skills (read-only: brainstorming, observability-check, prometheus, renovate-manager, teach, tempo; write: bkt, dashboarding, excalidraw-diagram, fiter-infrastructure-analyzer, grafana-oss, terraform-example-gen). Documented in README ("Skill write boundary") + identity.md (## Orchestration bullet). Independent validator PASS, zero misclassifications.
- **Phase 2 DONE (skill-subsystem):** `scripts/validate-skills.sh` (6 checks: SKILL.md exists, frontmatter open/close, name+description keys, metadata.tier valid, >500-line WARN, uppercase-{{PLACEHOLDER}} ERROR — lowercase {{ }} allowed as content). `scripts/tests/test_validate_skills.sh` (19 assertions). Wired into `run-tests.sh` as a real-tree gate (exit-code gated, warn tally). Real store: 12 OK + 1 advisory WARN (excalidraw 554 lines). Code-review validator PASS; applied its non-blocking fixes (exit-2 gating, 2-space tier guard, test-gap assertions).
- **Branch/PR:** this is a feature → branch `feat/skill-subsystem`, draft PR #8 (https://github.com/seyio91/ai-memory-system/pull/8). Phases 1–2 committed in 3 logical commits (planning → Phase 1 → Phase 2). Continue Phases 3–5 on this branch; mark PR ready when complete. NOTE: `skills/fiter-infrastructure-analyzer` is gitignored → its tier is applied locally but not in the PR (11/12 skills tracked).
- **Phase 3 DONE (skill-subsystem #11)** — commits `ba911df` (engine) + `19e681e` (Claude trigger + hardening), pushed. Engine `skill-boundary-check.sh` (snapshot/check, content-hash baselines, target-tree + memory-confinement halves). Claude in-session triggers: `claude/hooks/skill_boundary_marker.sh` (PostToolUse:Skill) + `skill_boundary_check.sh` (Stop, others-only memory + registered target, exit 2, set +e). Wired in `claude/settings.hooks.json`. Two validator rounds (FAIL→fixed→PASS); 35 assertions; suite 16 files green. Decisions: single-turn coverage; SubagentStop deferred; Codex trigger DEFERRED.
- **Phase 4 DONE** — commit `85c4d66`, pushed. `scripts/new-skill.sh` (scaffold-to-schema) + `scripts/install-skill.sh` (intake/normalize external skill; --tier required; no self-rating injection; normalizer conforms non-2-space metadata to our 2-space convention). Validator FAIL→fixed→PASS (data-loss guard, frontmatter-corruption fix). 47-assertion test. Suite 17 files green.
- **Next:** Phase 5 (self-rating + minimal partials #6/#5, FIRST-PARTY WORKFLOW SKILLS ONLY: renovate-manager, observability-check, fiter-infrastructure-analyzer, brainstorming; feedback lands in skill's own folder; remote skills get it only on request, demarcated block). Then inline quick wins #1 (resume_point in /checkpoint) & #7 (Two-Path principle doc).
- **USER STEP — live hook activation (not done by PR):** symlink `claude/hooks/skill_boundary_{marker,check}.sh` into `~/.claude/hooks/` + merge PostToolUse(Skill)+Stop from `claude/settings.hooks.json` into `~/.claude/settings.json`, restart session, verify hooks fire (couldn't integration-test).
- **Blockers:** none. Branch `feat/skill-subsystem`, draft PR #8 (Phases 1–3 done).

### 2026-06-30 — Skill subsystem build: Phases 1–4 done, Phase 5 next (pre-compaction)

**Task:** Build the POS-derived skill subsystem on branch `feat/skill-subsystem` (PR #8) — Phases 1–4 landed; Phase 5 (self-rating) + two inline quick wins remain.

**Done:**
- **Phase 1** (#10) — `metadata.tier` on all 12 skills (read-only: brainstorming, observability-check, prometheus, renovate-manager, teach, tempo; write: bkt, dashboarding, excalidraw-diagram, fiter-infrastructure-analyzer, grafana-oss, terraform-example-gen). Docs in README + identity.md. Validator PASS.
- **Phase 2** (#4) — `scripts/validate-skills.sh` (6 checks) + 19-assert test, wired into `run-tests.sh`. Validator PASS.
- **Phase 3** (#11) — `scripts/skill-boundary-check.sh` engine (snapshot/check, content-hash baselines, target-tree + memory-confinement halves) + Claude hooks `claude/hooks/skill_boundary_marker.sh` (PostToolUse:Skill) & `skill_boundary_check.sh` (Stop, others-only memory + registered target, exit 2, `set +e`); wired in `claude/settings.hooks.json`. Target-half convention: skill writes `skills/<self>/.boundary-target`. 35 assertions, two validator rounds (FAIL→PASS). Commits `ba911df`+`19e681e`.
- **Phase 4** (#12/#13) — `scripts/new-skill.sh` (scaffold) + `scripts/install-skill.sh` (intake/normalize; `--tier` required; no self-rating injection; normalizer conforms non-2-space metadata to 2-space). 47 assertions, validator FAIL→PASS. Commit `85c4d66`.
- All four phases committed AND pushed; working tree clean; full suite **17 files green** (lint clean, skills clean). PR #8 body shows Phases 1–4 done.

**Next:**
- **Phase 5** (#6/#5) — minimal partial mechanism + self-rating/improvement-loop block, applied to **first-party workflow skills only**: renovate-manager, observability-check, fiter-infrastructure-analyzer, brainstorming. Feedback lands in each skill's own folder (`skills/<name>/`); aggregation = `skills/*/` glob. Remote/imported skills get the block only on explicit request, as a demarcated appended section (fork-safe).
- Then inline quick wins: **#1** add a `resume:` line to the `/checkpoint` template (`claude/commands/checkpoint.md`); **#7** record the Two-Path principle as an authoring rule in README/identity.md.
- When all phases done: mark PR #8 ready for review.

**Blockers:**
- None blocking Phase 5. **User step (not done by PR):** live hook activation — symlink `claude/hooks/skill_boundary_{marker,check}.sh` into `~/.claude/hooks/` + merge PostToolUse(Skill)+Stop from `claude/settings.hooks.json` into `~/.claude/settings.json`, restart, verify hooks fire (couldn't integration-test). **Deferred by decision:** Codex executor trigger for #11 (Codex runs target-write work). **Advisory:** `excalidraw-diagram` SKILL.md 554 lines (>500).

### 2026-06-30 — Skill subsystem COMPLETE: Phase 5 + quick wins landed, PR #8 ready

**Task:** Finish the skill subsystem — Phase 5 (self-rating loop) and the two inline quick wins — and take PR #8 out of draft.

**Done:**
- **Phase 5 (#5/#6)** built, hardened, committed (`c48c1c0`), pushed: `scripts/partials/self-rating.md`, `apply-partial.sh` (marker-delimited inject/sync, `--force` first-injection guard, `--all` re-sync, idempotent), `skill-ratings.sh` (aggregator, `LC_ALL=C` decimal fix), `_lib.sh:skills_with_partial`. Creator wired (`new-skill.sh --kind workflow` injects; installer never does). Applied to the four first-party skills; on-request-only.
- **Membership refactor** (user call): dropped the static `FIRST_PARTY_SKILLS` list — loop membership is now DERIVED from the block marker, no drift.
- **Validator PASS** on all 8 criteria; took 3 hardening fixes from its adversarial pass (out-of-range-only logs surface as unrated; marker detection keys on full `<!-- partial:` prefix; dropped vestigial `--first-party` alias). `test_skill_ratings.sh` 34, `test_skill_creator.sh` 49. Full suite **18 files green**.
- **Quick wins:** #1 `/checkpoint` `Resume:` field; #7 Two-Path principle in README "Mental model" + identity.md.
- **Docs:** README "Self-rating loop" subsection; identity.md self-rating + Two-Path bullets.
- **PR #8** body updated (all 5 phases + quick wins) and **marked ready for review** (no longer draft).

**Next:**
- Awaiting user review/merge of PR #8.
- After merge: **user-side live activation** of the boundary hooks (symlink + settings merge + restart + verify) — still can't be integration-tested from here.
- On merge: archive `plans/skill-subsystem.md` → `archive/plans/` (all its todo items are ticked). `todo.md` is NOT fully ticked — the separate `state-snapshot` plan (#8) is still open, so don't roll todo yet.

**Blockers:**
- None. Deferred by decision: Codex executor trigger for #11. Advisory: `excalidraw-diagram` 554 lines.

**Resume:** Skill subsystem is done & PR #8 is ready — nothing to code. If continuing the broader backlog, open `projects/ai-memory/plans/state-snapshot.md` (#8, derived `.state/snapshot` — plan only, never built) and start at its Phase 1 (derivation sources + script-vs-flag decision).

### 2026-06-30 — State snapshot built; PR #9 open (POS thread done pending merge)

**Task:** Build the derived cross-project "In Flight" state snapshot (#8) — the last POS-adoption item.

**Done:**
- All 3 phases built on branch `feat/state-snapshot`, **PR #9** opened (not yet merged). `scripts/regenerate-state.sh` (derived table, mtime not git-log because 13/14 projects gitignored, on-demand/not-injected, `--stdout`, idempotent), `/state` command, README note, `.gitignore /state.md`, `test_regenerate_state.sh` (21 assertions). Suite **19 files green**.
- Independent **Validator PASS** on all 8 criteria; fixed its one finding (fenced-code `- [ ]` false-count) with a regression test.

**Next:**
- Await review/merge of **PR #9**.
- **On merge — the POS thread is fully done; execute the deferred cleanup:**
  1. Archive `plans/state-snapshot.md` → `archive/plans/` (status done, merged PR #9).
  2. **Move `wikis/pos-comparison.md` + `wikis/pos-adoption-backlog.md` → `archive/wikis/`** (the held directive — they were plan references, not system-component wiki; safe to move once #9 lands since state-snapshot was the last plan referencing the backlog).
  3. Record the state snapshot as an Architecture Decision in `memory.md`; prune the todo state-snapshot section.

**Blockers:**
- None. Both `feat/skill-subsystem` (PR #8, merged) and `feat/state-snapshot` (PR #9, open) are the POS-adoption deliverables; #9 closes the thread.

**Resume:** Nothing to code — `feat/state-snapshot` is pushed and PR #9 is open. When it merges, run the 3-step POS cleanup above (archive the plan, move both POS wikis to `archive/wikis/`, add the memory.md decision). The wiki move is the directive you've been holding.

### 2026-06-30 — POS-adoption thread CLOSED

**Task:** Post-merge cleanup after PR #9 (state snapshot) — close out the POS-adoption thread.

**Done:**
- PR #9 merged to main (`c655151`); branch deleted. `/state` symlink valid on main.
- Cleanup committed + pushed (`cb99a36`): archived `state-snapshot.md` → `archive/plans/`; moved both POS triage wikis → `archive/wikis/` (the held directive — done now that the last referencing plan shipped); recorded the `/state` decision + the "wikis = system components, not plan references" principle in `memory.md`; rolled the fully-ticked `todo.md` → `archive/todos/2026-06-30-pos-adoption.md` and reset.
- `wikis/` is now empty; no open plan-tier work remains.

**Next:**
- None queued. Optional: `/promote-memory` to drain this `working.md` (it's accumulated several checkpoints and re-ships every prompt).

**Blockers:**
- None. Both POS deliverables (skill subsystem PR #8, state snapshot PR #9) shipped and merged; thread complete.

**Resume:** Clean slate — `todo.md` is reset (no active work), both POS plans archived, wikis moved. If picking up new work, start from a fresh request. This `working.md` could be drained via `/promote-memory`.
