# Working — ai-memory

## Cross-project learnings (pending promotion)

_(none yet)_

## Checkpoints

### 2026-06-29 — Pulled remote; consolidated claude-memory-system → ai-memory; relative repo_path
**Task:** Pull+validate remote; rename local `claude-memory-system` to the now-tracked `ai-memory` meta-project; make `repo_path` env-portable.
**Done:**
- Fast-forward pull (13 commits) — clean, no conflicts.
- Consolidated `claude-memory-system` into `ai-memory`: union-then-trim `memory.md`; moved archive (14 plans/12 todos/2 working) + `working.md`; flipped `.claude/memory-project` → `ai-memory`; removed old project dir.
- 12 absolute `repo_path` values → relative to `AI_MEMORY_PROJECTS_ROOT`.
- ai-memory `repo_path: $MEMORY_DIR` sentinel — taught `resolve_repo_path` + `lint-memory.sh` to resolve `$MEMORY_DIR`(/sub) against the memory tree (machine-independent). Added 2 test_lib cases.
**Decision:** the self-referential ai-memory project resolves its checkout via `$MEMORY_DIR`, not `AI_MEMORY_PROJECTS_ROOT` — so the committed value is identical on every machine. (Promote to memory.md Decisions if it sticks.)
**Tests/validate:** `test_lib` 19/19, `test_lint_memory` 14/14, lint exit 0. Pre-existing (not mine): `test_executor.sh` 3 fail + `test_taskctl.sh` 4 fail — environmental (codex absent / taskctl override), confirmed in clean env.
**Merged:** PR #3 → main (`67c94c1`); local main synced, branch deleted. Decision promoted into `memory.md` Architecture Decisions ($MEMORY_DIR self-referential exception).
**Next:** `plans/consolidate-into-ai-memory.md` is done-stamped (kept in `plans/`, not archived, to avoid tracked-deletion churn). Pending cleanup item from github-core-migration still open.
**Blockers:** None.

### 2026-06-29 — Packaged for sharing; AI_MEMORY_PROJECTS_ROOT config decision
**Task:** Publish the memory system as a shareable repo and reconcile a branched fork.
**Done:**
- Pushed to github.com/seyio91/ai-memory-system (private, main): .gitignore, templates (identity/index/domain), vendored claude/ wiring (hooks/commands/CLAUDE.md/settings.hooks.json), install.sh, LICENSE, self-locating scripts, README rewrite.
- Captured backlog task 38ef6850 — agent-agnostic + '@'-sign on-demand/section-level context loading.
- Confirmed AI_MEMORY_PROJECTS_ROOT already exists (projects_root / resolve_repo_path in _lib.sh, reverse-map via memory-pin).
**Decision (IMPLEMENTED 2026-06-29, commit d42cec7):** Per-environment config loader — `_lib.sh` and `taskctl` source a gitignored `config.local.sh` next to the tree (sets AI_MEMORY_PROJECTS_ROOT, MEMORY_TASK_PROVIDER, …); shipped `config.local.sh.example`; committed default changed `$HOME/Downloads/personal` → `$HOME/Projects`. This machine keeps its real root via a local `config.local.sh`. This config-file pattern is the override seam for the agent-agnostic task (38ef6850).
**Gotcha:** `_lib.sh` self-locates `MEMORY_DIR` via `BASH_SOURCE` — correct under bash (how scripts run), but empty under zsh (the Bash-tool's interactive shell), so manual `. scripts/_lib.sh` from zsh mis-resolves. Not a real bug; just don't sanity-check it from zsh.
**Tests:** config loader covered by scripts/tests/test_config_local.sh (006b3bd, 5 cases); self-locating MEMORY_DIR default covered in test_lib.sh (58abc31). Full suite 11 files green in a clean env.
**Next:** Pick up agent-agnostic + '@'-loading (task 38ef6850) when ready.
**Blockers:** None.
