# Executor deny-list — destructive/additive operations to running infrastructure.
# TRACKED DEFAULTS: do not hand-edit this file on an installed instance. Editing
# it dirties the tracked tree and sync-system.sh:dirty_tracked_guard will refuse
# to sync. Add instance-local rules to scripts/deny-list.local.txt instead.
# That file is gitignored and additive-only; there is no un-deny syntax.
#
# Format: one "<binary> <subcommand...>" spec per line. `#` lines and blanks
# are ignored.
terraform apply
terraform destroy
kubectl apply
kubectl delete
helm install
helm upgrade
helm uninstall
helm delete
gh pr merge
bkt pr merge
az repos pr update
